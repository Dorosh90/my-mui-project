import { Container, Typography } from '@mui/material';

const About = () => {
  return (
    <Container>
      <Typography variant="h2" gutterBottom>
        О нас
      </Typography>
      <Typography variant="body1">
        Мы - молодая компания, которая предоставляет отличные услуги в области веб-разработки.
      </Typography>
    </Container>
  );
};

export default About;
